# Leia um valor e: Converta para float; Mostre a metade

num = input('Digite um número: ')
num_convertido = float(num)
print('O número escolhido foi {:.2f} e o dobro é {}'.format(num_convertido, num_convertido *2))